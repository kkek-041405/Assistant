package com.kkek.assistant.core

import javax.inject.Inject

class Dispatcher @Inject constructor() {
    fun dispatch(command: String) {
        // Placeholder for dispatching commands
        println("Dispatching command: $command")
    }
}
